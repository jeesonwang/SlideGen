# API Configuration
# In local Vite development, leave API base empty so requests use the /api proxy.
# Set VITE_API_BASE_URL only when you explicitly need to bypass the dev proxy.

# Application Name
VITE_APP_NAME=SlideGen
